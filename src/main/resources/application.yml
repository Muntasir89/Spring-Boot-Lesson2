spring:
  datasource:
    username: root
    password: password
    url: jdbc:mysql://localhost:3306/schooldb
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true